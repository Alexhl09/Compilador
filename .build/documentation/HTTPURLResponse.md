# Extensions on HTTPURLResponse

## Properties

### `headers`

Returns `allHeaderFields` as `HTTPHeaders`.

``` swift
public var headers: HTTPHeaders 
```

### `headers`

Returns `allHeaderFields` as `HTTPHeaders`.

``` swift
public var headers: HTTPHeaders 
```

### `headers`

Returns `allHeaderFields` as `HTTPHeaders`.

``` swift
public var headers: HTTPHeaders 
```
