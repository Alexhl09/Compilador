# AssertRoundtrip(for:file:line:)

Ensures a given render node can be encoded and decode back without throwing.

``` swift
public func AssertRoundtrip(for renderNode: RenderNode, file: StaticString = #file, line: UInt = #line) 
```
