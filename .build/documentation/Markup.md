# Extensions on Markup

## Properties

### `seeAlso`

``` swift
@available(*, deprecated, message: "Use DocumentationMarkup(markup:) to parse structured markup")
    public var seeAlso: SeeAlsoSection? 
```
